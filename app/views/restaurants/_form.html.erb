<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :category, prompt: "Select a category", collection: Restaurant::CATEGORIES %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.submit %>
<% end %>
